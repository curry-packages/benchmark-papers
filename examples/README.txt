Some simple examples for benchmark papers
=========================================

Generate and show the papers by

    > exec-benchmarkpaper -f <paper>.tex


Examples:

- nrev_table : a simple example to benchmark naive reverse and show it
  in a table

- nrev_plots: a naive reverse benchmark for different Curry systems
  where the result is shown as a graphic generated via gnuplot
